#include "BitTools.h"
#include <string>       // std::string
#include <iostream>     // std::cout
#include <sstream> 

using namespace std;
/*
string BitTools::printBinary(const short num) const
{
    string out;
    for(int i = 0; i < sizeof(short)*8; i++)
    {
        if(((num >> i) & 0x1) == 1)
        {
            out.insert(0, "1");
        }
        else
        {
            out.insert(0, "0");
        }
    }
        return out;
}
*/
